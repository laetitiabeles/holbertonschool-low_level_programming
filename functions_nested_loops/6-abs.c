#include "main.h"
#include <stdio.h>

/**
* _abs - entry point
*
* @n: value to check
*
* Description: program that prints the absolute value
*
* Return: always 0 (success)
*/

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (0);
}
}
