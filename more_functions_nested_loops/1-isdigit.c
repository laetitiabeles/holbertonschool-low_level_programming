#include "main.h"

/**
* _isdigit - Entry point
* @c: le caractère à vérifier
* Description: program that check if c is lower or upper
* Return: always 0
*/

int _isdigit(int c)
{

for (c = 0; c <= 9; c++)
{
return (1);

}
return (0);
}
